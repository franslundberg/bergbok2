# Integration

This directory is not a sixth module. It contains only cross-module contract
tests and executable thin-workflow demos.

Run `npm run test:integration` for the public-interface composition test or
`npm run demo` for the complete inspectable workflow demonstration.
